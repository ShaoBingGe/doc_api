# OS
.DS_Store
**/.DS_Store

# Python
__pycache__/
**/__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.venv/
venv/
*.egg

# Node
node_modules/
dist/
.npm/

# Environment & secrets
.env
.env.*
!.env.example
backend/.env

# Database
*.db
*.sqlite
*.sqlite3
*.db-shm
*.db-wal
backend/data/

# Uploads / static files
backend/static/uploads/

# Logs
*.log

# IDE
.vscode/
.idea/
*.swp
*.swo

# Claude local settings
.claude/settings.local.json
.claude/worktrees/

# Embedded repos (not submodules)
label-studio/
label-studio-ml-backend/

# Test artifacts / 本地测试样本（含真实发票 PII + 体积大，勿入库；
# 与 golden_set 同策略：原始扫描不入库，仅保留 README 说明角色）
testing/
MY-test/

# 根目录散件：真实票据 PDF（PII）与未接线的模板草稿
# （newMY*.yaml 不匹配 template_loader 的 *_invoice_prompt.yaml glob，
#   不会被加载——正式模板请按 <COUNTRY>_invoice_prompt.yaml 命名）
/*.pdf
/newMY*.yaml

# 会话记忆（个人工作区文件，不入库）
memory/

# Golden eval set: keep GT answer key + manifest + exporter in git, but NOT the
# raw invoice scans (PII + binary + regenerable via build_golden_set.py).
backend/app/ocr_optimizer/eval/golden_set/*/docs/
_latest_eval.json
app/ocr_optimizer/eval/golden_set/*/_latest_eval.json

# 本地测试数据 / 样本发票语料（体积大、含真实数据，勿入库）
Japan-inv/
# 根级运行时残留（真实上传在 backend/static/uploads）
/data/

# 微软 SkillOpt 参考项目（第三方，仅供研究，勿入库）
skillopt/
backend/ab_prompt_test.py

# 开放平台验证产物（含生产 client_secret / access_token，严禁入库）
_postman/*environment*.json
!_postman/*environment*.example.json
_postman/newman_*.json
_postman/retry*.json
_postman/largest*.json
_postman/over16.json
_postman/recheck*.json
_postman/regress*.json
_postman/diag_*.json
_postman/table_layout.json
